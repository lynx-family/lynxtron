---
'@lynx-js/lynxtron-dev-plugins': patch
---

Preserve production dependency boundaries in host builds so native npm packages can load their addons. Stop development runtime processes and cancel pending restarts when the compiler or development process closes.
