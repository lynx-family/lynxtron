import os
import platform

machine = platform.machine().lower()
machine = "x86_64" if machine == "amd64" else machine
system = platform.system().lower()

deps = {
    '../lynx': {
        'type': 'solution',
        'url': 'git@github.com:lynx-family/lynx.git',
        'commit': 'a4c22e545cb0e0e948925cfcf1f2f22bd9d84c1a',
        "deps_file": "dependencies/DEPS.lynxtron",
        "ignore_in_git": True,
    },
}
